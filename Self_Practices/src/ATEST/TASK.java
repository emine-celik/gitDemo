package ATEST;

import jdk.swing.interop.SwingInterOpUtils;
import org.w3c.dom.ls.LSOutput;

import javax.print.attribute.standard.PagesPerMinute;
import java.time.format.DateTimeFormatter;
import java.util.Arrays;
import java.util.Scanner;

public class TASK {
  /*  Here is our challenge for today and we will go over together tonight.
    Write a program ask user to create a password and the requirements for password are:
            1. the length of password must be between 8 to 32;
 2. Must contain at least one Uppercase and one lower case letter;
 3. Must contain at least one number

    test231tes
*/

////////////////////////////////////////////////////////////////

    public static void main(String[] args) {


        Scanner scan=new Scanner(System.in);
        System.out.println("Enter your password number");
        String password=scan.next();

        if(password.length() > 8 || password.length() <= 32){

            

        }













    }






    }